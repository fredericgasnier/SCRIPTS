pipeline {
    agent any

    stages {
        if (Param.equals("mat")) {
        stage('Init') {
            steps {
                echo "Testing..."
            }
        }
        stage('Build') {
            steps {
                 echo "Building..."
            }
        }
        stage('Deploy') {
            steps {
                 echo "Code deployed."
            }
        }
        }
    }
}
